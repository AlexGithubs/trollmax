/** Line break between hook and URL — works well in Messages, WhatsApp, etc. */
export function shareMessageWithUrl(shareUrl: string): string {
  return `Check out what I made on Trollmax.xyz!\n${shareUrl}`
}
